extends layout

block content
  .center
    h1
      a(href='/')
        = message

    h2 -

    h1
      a(href='/')
        i.fa.fa-home
        |  Let me go !


  - if (!(typeof error === 'undefined'))
    h2= error.status
    pre #{error.stack}
